1) Train the model with: rasa train
2) Start the model in debug mode: rasa shell --debug
2b) On a different terminal go start the action server with: rasa run actions
3) Type input
Note: Currently the only input that does anything is user input of an order, so
I'll have a burger.

